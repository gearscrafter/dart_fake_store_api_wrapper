name: dart_fake_store_api_wrapper
description: The package simplifies operations using the Fake Store API.
version: 0.0.2
repository: https://github.com/gearscrafter/dart_fake_store_api_wrapper.git

environment:
  sdk: ^3.4.1

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  http: ^1.2.1
  dartz: ^0.10.1

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
  mockito: ^5.4.4
  build_runner: ^2.4.11
